# Migrations archivées

Toutes les migrations 001 → 144 (+ 008b) sont déjà appliquées en production.
Conservées ici en référence historique pour comprendre l'évolution du schéma
(grep, blame, audit).

**Ne pas y toucher.** Toute nouvelle migration va dans le dossier parent
(`supabase/migrations/`) avec un numéro ≥ 145, et est appliquée manuellement
via le SQL Editor Supabase (cf. CLAUDE.md).

Pour lire le schéma courant complet : `docs/supabase-context.md`.
